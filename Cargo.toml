[package]
name = "xcb"
version = "0.6.0"
authors = [
    "James Miller <james@aatch.net>",
    "Remi Thebault <remi.thebault@gmail.com>",
    "Thomas Bracht Laumann Jespersen <laumann.thomas@gmail.com>"
]
description = "Rust bindings and wrappers for XCB"
repository = "https://github.com/rtbo/rust-xcb"
readme = "README.md"
keywords = ["xcb", "window", "xlib", "x11", "opengl"]
license = "MIT"
exclude = [ "examples/*", "scripts/*" ]
build = "build.rs"

[build-dependencies]
libc = "0.2"

[lib]
name = "xcb"
path = "src/xcb.rs"

[dependencies]
libc = "0.2"

[dependencies.x11]
version = "2.3"
optional = true
features = ["xlib"]

[features]
xlib_xcb = ["x11/xlib"]
bigreq = []
composite = [ "xfixes" ]
damage = [ "xfixes" ]
dpms = []
dri2 = []
dri3 = []
ge = []
glx = []
present = [ "render", "xfixes", "sync" ]
randr = [ "render" ]
record = []
render = []
res = []
screensaver = []
shape = []
shm = []
sync = []
xc_misc = []
xevie = []
xf86dri = []
xf86vidmode = []
xfixes = [ "render", "shape" ]
xinerama = []
xinput = [ "xfixes" ]
xkb = []
xprint = []
xselinux = []
xtest = []
xvmc = [ "xv" ]
xv = [ "shm" ]

# ge and xf86vidmode are apparently nor built neither installed by libxcb C project
# extension xinput is broken
# all others are in debug_all whose purpose is to check build of extensions
debug_all = [ "bigreq", "composite", "damage", "dpms", "dri2", "dri3",
        "glx", "randr", "record", "render", "res", "screensaver", "shape",
        "shm", "xc_misc", "xevie", "xf86dri", "xfixes", "xinerama", "xkb",
        "xprint", "xselinux", "xtest", "xvmc", "xv" ]



# cargo does not seam to support example.features yet.
# That is addressed in cargo/#1570
#[[example]]
#name = "randr_crtc_info"
#features = "randr"
